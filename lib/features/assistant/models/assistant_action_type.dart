/// Proposed (never executed) actions returned by the assistant.
enum AssistantActionType {
  reviewDraft,
  createEvent,
  createQuote,
  askQuestion,
  checkSchedule,
  checkAvailability,
  searchClient,
  updateEvent,
  none,
}
