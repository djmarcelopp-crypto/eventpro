/// Entity kinds the local parser may extract from natural language.
enum AssistantEntityType {
  eventType,
  date,
  startTime,
  endTime,
  city,
  venue,
  guestCount,
  clientName,
  equipment,
  service,
  teamRequirement,
  vehicleRequirement,
  budget,
  notes,
  duration,
}
